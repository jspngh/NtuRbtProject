#include <iostream>
#include <unistd.h>

#include "robot.hpp"

using namespace std;

int main(int, char**)
{
    Robot r;
    for (int i = 0; i < 7; i++)
    {
        // r.setSpeed(50, 5);
        // r.resetJoints();
        // r.openGripper();
        // r.sleep(5 * 1000);
        // r.closeGripper();
        // r.sleep(2 * 1000);
        // r.dropPiece(i);
        // r.setSpeed(50, 5);
    }
    return 0;
}
